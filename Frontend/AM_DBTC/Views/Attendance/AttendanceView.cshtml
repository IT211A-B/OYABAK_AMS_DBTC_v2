@model AM_DBTC.Models.AttendanceModel
@{
    Layout = "_AttendanceLayout";
}

<div class="attendance-container">

    @* header *@
    <div class="attendance-header">
        <h1>Attendance</h1>
    </div>

    @* filter card *@
    <div class="attendance-search-card">
        <form class="attendance-search-form">

            @* search *@
            <input type="text"
                   id="studentNameSearch"
                   class="attendance-search-input"
                   placeholder="Search by Student Name"
                   value="@Model.SearchUserName" />

            @* courses *@
            <select id="courseFilter" class="attendance-select">
                @foreach (var c in Model.Courses)
                {
                    <option value="@c" selected="@(Model.SelectedCourse == c ? "selected" : null)">
                        @c
                    </option>
                }
            </select>

            @* months *@
            <select id="monthFilter" class="attendance-select">
                @foreach (var m in Model.Months)
                {
                    <option value="@m" selected="@(Model.SelectedMonth == m ? "selected" : null)">
                        @m
                    </option>
                }
            </select>

            @* filter button *@
            <button type="button" class="attendance-search-btn">
                Filter
            </button>

        </form>
    </div>

    @* summary cards *@
    <div class="summary-cards">
        <div class="summary-card total-card">
            <h2>@Model.TotalClasses</h2>
            <p>total classes held</p>
        </div>

        <div class="summary-card present-card">
            <h2>@Model.ClassesPresent</h2>
            <p>classes present</p>
        </div>

        <div class="summary-card absent-card">
            <h2>@Model.ClassesAbsent</h2>
            <p>classes absent</p>
        </div>
    </div>

    @* table *@
    <div class="attendance-card">

        <h2 class="attendance-title">Attendance Records</h2>

        <table class="attendance-table">
            <thead>
                <tr>
                    <th>Student Name</th>
                    <th>Date</th>
                    <th>Day</th>
                    <th>Status</th>
                    <th>Time</th>
                    <th>Course</th>
                </tr>
            </thead>

            <tbody>
                @if (Model.Records.Count == 0)
                {
                    <tr>
                        <td colspan="6" style="text-align:center; padding:30px; color:gray;">
                            No Records Found.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var record in Model.Records)
                    {
                        var status = record.Status?.ToLower() ?? "";

                        <tr>
                            <td>@record.UserName</td>
                            <td>@record.Date.ToString("M/d")</td>
                            <td>@record.Day</td>

                            <td>
                                @{
                                    var statusClass = status switch
                                    {
                                        "present" => "present",
                                        "absent" => "absent",
                                        "leave" => "leave",
                                        _ => ""
                                    };

                                    var statusIcon = status switch
                                    {
                                        "present" => "✓ ",
                                        "absent" => "✕ ",
                                        "leave" => "◴ ",
                                        _ => ""
                                    };
                                }

                                <span class="status @statusClass">
                                    @statusIcon@record.Status
                                </span>
                            </td>

                            <td>@record.Time</td>
                            <td class="course-name">@record.Course</td>
                        </tr>
                    }
                }
            </tbody>
        </table>

        @* pagination *@
        <div class="pagination">
            <button type="button"
                    class="page-btn"
                    onclick="changePage(@(Model.CurrentPage - 1))"
                    @(Model.CurrentPage <= 1 ? "disabled" : "")>
                ‹ Previous
            </button>

            <span class="page-number">
                Page @Model.CurrentPage of @Model.TotalPages
            </span>

            <button type="button"
                    class="page-btn"
                    onclick="changePage(@(Model.CurrentPage + 1))"
                    @(Model.CurrentPage >= Model.TotalPages ? "disabled" : "")>
                Next ›
            </button>
        </div>

    </div>
</div>

@section Scripts {
    <script src="~/js/AttendanceAjax.js"></script>
}